name: Bug report
description: Report a reproducible problem in a herdr-win snapshot.
title: "[bug] "
body:
  - type: markdown
    attributes:
      value: |
        **Before you start:** Read [CONTRIBUTING.md](https://github.com/hdosys/herdr-win/blob/master/CONTRIBUTING.md).

        Use this form for a problem observed in a herdr-win Windows snapshot. If practical, try the same workflow with upstream Herdr and report the result below. Do not report a fork-only failure to upstream.

        Keep the report factual and include only the smallest relevant logs. For general Herdr questions and product feature requests, use the upstream project links shown on the new-issue page.

  - type: checkboxes
    id: bug-confirmation
    attributes:
      label: Is this a reproducible bug?
      description: Confirm that the problem occurs in a herdr-win snapshot and has exact reproduction steps.
      options:
        - label: I confirm this is a reproducible herdr-win bug, not a general Herdr question or feature request.
          required: true

  - type: textarea
    id: current-behavior
    attributes:
      label: Current behavior
      description: What is happening now? Describe what you observed, not a guessed cause.
    validations:
      required: true

  - type: textarea
    id: expected-behavior
    attributes:
      label: Expected behavior
      description: What should happen instead?
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: Reproduction
      description: What are the shortest exact steps or commands that show the problem?
    validations:
      required: true

  - type: textarea
    id: impact
    attributes:
      label: Impact
      description: How does this affect you?
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: Copy the release tag and upstream revision from the herdr-win release notes.
      value: |
        - herdr-win release tag:
        - Upstream revision:
        - Windows version:
        - Terminal:
        - Shell:
        - Result with upstream Herdr (same, not present, or not tested):
        - Relevant config, if any:
    validations:
      required: true
