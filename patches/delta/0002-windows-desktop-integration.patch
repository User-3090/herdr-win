From 5b1d5b5fba4ec44f4ffc21edfe88ca06853b8fcb Mon Sep 17 00:00:00 2001
From: User-3090 <71881005+User-3090@users.noreply.github.com>
Date: Wed, 29 Jul 2026 15:41:58 +0200
Subject: [PATCH 2/4] fix(windows): bound desktop integration lifetimes

---
 Cargo.toml              |   2 +
 src/platform/windows.rs | 227 ++++++++++++++++++++++++++++++++++------
 src/sound.rs            | 139 ++++++++++++++----------
 3 files changed, 281 insertions(+), 87 deletions(-)

diff --git a/Cargo.toml b/Cargo.toml
index 891bcbab56e488d68801bed3bee5b1eb91ef5c14..fdae5372ac8adb51e36494c48df1b4aa9ce1e467 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -50,12 +50,14 @@ portable-pty = { path = "vendor/portable-pty" }
 windows-sys = { version = "0.61.2", features = [
     "Wdk_System_Threading",
     "Win32_Foundation",
+    "Win32_Graphics_Gdi",
     "Win32_System_DataExchange",
     "Win32_System_Diagnostics_Debug",
     "Win32_System_Diagnostics_ToolHelp",
     "Win32_System_Console",
     "Win32_System_JobObjects",
     "Win32_System_Kernel",
+    "Win32_System_LibraryLoader",
     "Win32_System_Memory",
     "Win32_System_Ole",
     "Win32_System_Pipes",
diff --git a/src/platform/windows.rs b/src/platform/windows.rs
index 705a1d1ffbe62dfa35ae1e0bcfed76e7c6f57c3c..6923d1fac9e0aedecc5af285c42774ed35eaa0e5 100644
--- a/src/platform/windows.rs
+++ b/src/platform/windows.rs
@@ -4,7 +4,7 @@ use std::{
     mem::{size_of, MaybeUninit},
     path::PathBuf,
     ptr::{copy_nonoverlapping, null_mut},
-    sync::{Arc, Mutex},
+    sync::{Arc, Mutex, OnceLock},
     time::{Duration, Instant},
 };
 
@@ -26,6 +26,7 @@ use windows_sys::{
                 },
             },
             JobObjects::IsProcessInJob,
+            LibraryLoader::GetModuleHandleW,
             Memory::{GlobalAlloc, GlobalLock, GlobalUnlock, GMEM_MOVEABLE},
             Ole::CF_UNICODETEXT,
             Threading::{
@@ -43,12 +44,17 @@ use windows_sys::{
                 },
             },
             Shell::{
-                CommandLineToArgvW, ShellExecuteW, Shell_NotifyIconW, NIF_ICON, NIF_INFO, NIF_TIP,
-                NIIF_INFO, NIIF_NOSOUND, NIM_ADD, NIM_DELETE, NIM_MODIFY, NOTIFYICONDATAW,
+                CommandLineToArgvW, ShellExecuteW, Shell_NotifyIconW, NIF_ICON, NIF_INFO,
+                NIF_MESSAGE, NIF_TIP, NIIF_INFO, NIIF_NOSOUND, NIM_ADD, NIM_DELETE, NIM_MODIFY,
+                NIM_SETVERSION, NIN_BALLOONHIDE, NIN_BALLOONSHOW, NIN_BALLOONTIMEOUT,
+                NIN_BALLOONUSERCLICK, NOTIFYICONDATAW, NOTIFYICON_VERSION_4,
             },
             WindowsAndMessaging::{
-                CreateWindowExW, DestroyWindow, GetForegroundWindow, GetWindowThreadProcessId,
-                LoadIconW, SendMessageTimeoutW, IDI_APPLICATION, SMTO_ABORTIFHUNG, WM_IME_CONTROL,
+                CreateWindowExW, DefWindowProcW, DestroyWindow, DispatchMessageW,
+                GetForegroundWindow, GetMessageW, GetWindowThreadProcessId, KillTimer, LoadIconW,
+                PostQuitMessage, RegisterClassW, SendMessageTimeoutW, SetTimer, TranslateMessage,
+                HWND_MESSAGE, IDI_APPLICATION, MSG, SMTO_ABORTIFHUNG, WM_APP, WM_IME_CONTROL,
+                WM_TIMER, WNDCLASSW,
             },
         },
     },
@@ -83,6 +89,13 @@ struct ProcessSnapshotCache {
 
 static FOREGROUND_PROCESS_SNAPSHOT_CACHE: Mutex<ProcessSnapshotCache> =
     Mutex::new(ProcessSnapshotCache { cached: None });
+static NOTIFICATION_WINDOW_CLASS: OnceLock<Result<u16, i32>> = OnceLock::new();
+
+const NOTIFICATION_CALLBACK_MESSAGE: u32 = WM_APP + 0x45;
+const NOTIFICATION_ICON_ID: u32 = 1;
+const NOTIFICATION_TIMER_ID: usize = 1;
+const NOTIFICATION_QUEUE_TIMEOUT_MILLIS: u32 = 60_000;
+const NOTIFICATION_VISIBLE_TIMEOUT_MILLIS: u32 = 30_000;
 
 pub(crate) fn should_draw_host_cursor_by_default() -> bool {
     true
@@ -677,7 +690,7 @@ pub fn show_desktop_notification(title: &str, body: Option<&str>) -> std::io::Re
     let (ready_tx, ready_rx) = std::sync::mpsc::sync_channel(1);
     std::thread::Builder::new()
         .name("herdr-windows-notification".into())
-        .spawn(move || show_desktop_notification_on_thread(&title, &body, ready_tx))?;
+        .spawn(move || run_desktop_notification(&title, &body, ready_tx))?;
     ready_rx
         .recv_timeout(Duration::from_secs(2))
         .map_err(|err| match err {
@@ -691,53 +704,121 @@ pub fn show_desktop_notification(title: &str, body: Option<&str>) -> std::io::Re
         })?
 }
 
-fn show_desktop_notification_on_thread(
+fn run_desktop_notification(
     title: &str,
     body: &str,
     ready_tx: std::sync::mpsc::SyncSender<std::io::Result<bool>>,
 ) {
-    let class_name = wide_null("STATIC");
-    let window_name = wide_null("Herdr notifications");
+    let hwnd = match create_notification_window() {
+        Ok(hwnd) => hwnd,
+        Err(err) => {
+            let _ = ready_tx.send(Err(err));
+            return;
+        }
+    };
+
+    if let Err(err) = add_notification_icon(hwnd, title, body) {
+        unsafe {
+            DestroyWindow(hwnd);
+        }
+        let _ = ready_tx.send(Err(err));
+        return;
+    }
+
+    if unsafe {
+        SetTimer(
+            hwnd,
+            NOTIFICATION_TIMER_ID,
+            NOTIFICATION_QUEUE_TIMEOUT_MILLIS,
+            None,
+        )
+    } == 0
+    {
+        let err = std::io::Error::last_os_error();
+        remove_notification_icon(hwnd);
+        let _ = ready_tx.send(Err(err));
+        return;
+    }
+
+    if ready_tx.send(Ok(true)).is_err() {
+        remove_notification_icon(hwnd);
+        return;
+    }
+
+    if let Err(err) = notification_message_loop(hwnd) {
+        tracing::warn!(err = %err, "Windows desktop notification message loop failed");
+    }
+    remove_notification_icon(hwnd);
+}
+
+fn create_notification_window() -> std::io::Result<HWND> {
+    let module = unsafe { GetModuleHandleW(std::ptr::null()) };
+    if module.is_null() {
+        return Err(std::io::Error::last_os_error());
+    }
+
+    let class_atom = NOTIFICATION_WINDOW_CLASS.get_or_init(|| {
+        let class_name = wide_null("HerdrDesktopNotificationWindow");
+        let window_class = WNDCLASSW {
+            lpfnWndProc: Some(notification_window_proc),
+            hInstance: module,
+            lpszClassName: class_name.as_ptr(),
+            ..WNDCLASSW::default()
+        };
+        let atom = unsafe { RegisterClassW(&window_class) };
+        if atom == 0 {
+            Err(std::io::Error::last_os_error().raw_os_error().unwrap_or(1))
+        } else {
+            Ok(atom)
+        }
+    });
+    let class_atom = (*class_atom).map_err(std::io::Error::from_raw_os_error)?;
+    let atom_pointer = usize::from(class_atom) as *const u16;
     let hwnd = unsafe {
         CreateWindowExW(
             0,
-            class_name.as_ptr(),
-            window_name.as_ptr(),
+            atom_pointer,
+            std::ptr::null(),
             0,
             0,
             0,
             0,
             0,
+            HWND_MESSAGE,
             null_mut(),
-            null_mut(),
-            null_mut(),
+            module,
             std::ptr::null(),
         )
     };
     if hwnd.is_null() {
-        let _ = ready_tx.send(Err(std::io::Error::last_os_error()));
-        return;
+        Err(std::io::Error::last_os_error())
+    } else {
+        Ok(hwnd)
     }
+}
 
-    let mut notification = unsafe { std::mem::zeroed::<NOTIFYICONDATAW>() };
-    notification.cbSize = size_of::<NOTIFYICONDATAW>() as u32;
-    notification.hWnd = hwnd;
-    notification.uID = 1;
+fn add_notification_icon(hwnd: HWND, title: &str, body: &str) -> std::io::Result<()> {
+    let mut notification = notification_identity(hwnd);
     notification.hIcon = unsafe { LoadIconW(null_mut(), IDI_APPLICATION) };
-    notification.uFlags = NIF_TIP;
+    notification.uCallbackMessage = NOTIFICATION_CALLBACK_MESSAGE;
+    notification.uFlags = NIF_MESSAGE | NIF_TIP;
     if !notification.hIcon.is_null() {
         notification.uFlags |= NIF_ICON;
     }
     copy_wide_truncated(&mut notification.szTip, "Herdr");
 
     if unsafe { Shell_NotifyIconW(NIM_ADD, &notification) } == 0 {
-        let _ = ready_tx.send(Err(std::io::Error::other(
+        return Err(std::io::Error::other(
             "failed to add Herdr notification-area icon",
-        )));
-        unsafe {
-            DestroyWindow(hwnd);
-        }
-        return;
+        ));
+    }
+
+    notification.Anonymous.uVersion = NOTIFYICON_VERSION_4;
+    if unsafe { Shell_NotifyIconW(NIM_SETVERSION, &notification) } == 0 {
+        delete_notification_icon(hwnd);
+        return Err(std::io::Error::other(
+            "failed to set Herdr notification-area icon version",
+        ));
     }
 
     notification.uFlags = NIF_INFO;
@@ -745,20 +826,91 @@ fn show_desktop_notification_on_thread(
     copy_wide_truncated(&mut notification.szInfoTitle, title);
     copy_wide_truncated(&mut notification.szInfo, body);
     if unsafe { Shell_NotifyIconW(NIM_MODIFY, &notification) } == 0 {
+        delete_notification_icon(hwnd);
+        return Err(std::io::Error::other(
+            "failed to show Herdr desktop notification",
+        ));
+    }
+
+    Ok(())
+}
+
+fn notification_message_loop(hwnd: HWND) -> std::io::Result<()> {
+    let mut message = MSG::default();
+    loop {
+        let result = unsafe { GetMessageW(&mut message, hwnd, 0, 0) };
+        if result == -1 {
+            return Err(std::io::Error::last_os_error());
+        }
+        if result == 0 {
+            return Ok(());
+        }
         unsafe {
-            Shell_NotifyIconW(NIM_DELETE, &notification);
-            DestroyWindow(hwnd);
+            TranslateMessage(&message);
+            DispatchMessageW(&message);
         }
-        let _ = ready_tx.send(Err(std::io::Error::other(
-            "failed to show Herdr desktop notification",
-        )));
-        return;
     }
+}
 
-    let _ = ready_tx.send(Ok(true));
-    std::thread::sleep(Duration::from_secs(10));
+unsafe extern "system" fn notification_window_proc(
+    hwnd: HWND,
+    message: u32,
+    wparam: usize,
+    lparam: isize,
+) -> isize {
+    if message == NOTIFICATION_CALLBACK_MESSAGE {
+        match notification_event(lparam) {
+            NIN_BALLOONSHOW => unsafe {
+                SetTimer(
+                    hwnd,
+                    NOTIFICATION_TIMER_ID,
+                    NOTIFICATION_VISIBLE_TIMEOUT_MILLIS,
+                    None,
+                );
+            },
+            NIN_BALLOONHIDE | NIN_BALLOONTIMEOUT | NIN_BALLOONUSERCLICK => unsafe {
+                PostQuitMessage(0);
+            },
+            _ => {}
+        }
+        return 0;
+    }
+    if message == WM_TIMER && wparam == NOTIFICATION_TIMER_ID {
+        unsafe {
+            PostQuitMessage(0);
+        }
+        return 0;
+    }
+
+    unsafe { DefWindowProcW(hwnd, message, wparam, lparam) }
+}
+
+fn notification_event(lparam: isize) -> u32 {
+    (lparam as u32) & 0xffff
+}
+
+fn notification_identity(hwnd: HWND) -> NOTIFYICONDATAW {
+    NOTIFYICONDATAW {
+        cbSize: size_of::<NOTIFYICONDATAW>() as u32,
+        hWnd: hwnd,
+        uID: NOTIFICATION_ICON_ID,
+        ..Default::default()
+    }
+}
+
+fn delete_notification_icon(hwnd: HWND) {
+    let notification = notification_identity(hwnd);
     unsafe {
         Shell_NotifyIconW(NIM_DELETE, &notification);
+    }
+}
+
+fn remove_notification_icon(hwnd: HWND) {
+    unsafe {
+        KillTimer(hwnd, NOTIFICATION_TIMER_ID);
+    }
+    delete_notification_icon(hwnd);
+    unsafe {
         DestroyWindow(hwnd);
     }
 }
@@ -1218,6 +1370,13 @@ mod tests {
         assert_eq!(destination[5], 0);
     }
 
+    #[test]
+    fn windows_notification_event_uses_version_four_low_word() {
+        let lparam =
+            ((super::NOTIFICATION_ICON_ID as isize) << 16) | super::NIN_BALLOONSHOW as isize;
+        assert_eq!(super::notification_event(lparam), super::NIN_BALLOONSHOW);
+    }
+
     #[test]
     fn cmd_agent_command_encodes_edge_arguments_without_cmd_expansion() {
         use base64::Engine as _;
diff --git a/src/sound.rs b/src/sound.rs
index ec4106db091120b5ba646d07632d80caa55b425e..b22f1c1ea325a9e09beac060486e8513c57d673e 100644
--- a/src/sound.rs
+++ b/src/sound.rs
@@ -5,12 +5,12 @@
 //! players — no Rust audio dependencies.
 
 use std::io::Write;
-#[cfg(not(any(windows, target_os = "macos")))]
+#[cfg(not(target_os = "macos"))]
 use std::io::{Read, Result as IoResult};
 use std::path::{Path, PathBuf};
 use std::process::{Command, Output};
 use std::sync::atomic::{AtomicU64, Ordering};
-#[cfg(not(any(windows, target_os = "macos")))]
+#[cfg(not(target_os = "macos"))]
 use std::time::{Duration, Instant};
 
 use tracing::warn;
@@ -20,7 +20,9 @@ const DISABLE_SOUND_ENV: &str = "HERDR_DISABLE_SOUND";
 const WINDOWS_SOUND_PATH_ENV: &str = "HERDR_SOUND_PATH";
 #[cfg(not(any(windows, target_os = "macos")))]
 const AUDIO_PLAYER_TIMEOUT: Duration = Duration::from_secs(15);
-#[cfg(not(any(windows, target_os = "macos")))]
+#[cfg(windows)]
+const WINDOWS_AUDIO_PLAYER_TIMEOUT: Duration = Duration::from_secs(20);
+#[cfg(not(target_os = "macos"))]
 const AUDIO_PLAYER_POLL_INTERVAL: Duration = Duration::from_millis(25);
 
 static SOUND_TMP_COUNTER: AtomicU64 = AtomicU64::new(0);
@@ -186,9 +188,12 @@ fn windows_player_command(path: &Path) -> Command {
 
 #[cfg(windows)]
 fn run_windows_player(path: &Path) -> Result<Output, String> {
-    windows_player_command(path)
-        .output()
-        .map_err(|e| format!("Windows MediaPlayer playback failed: {e}"))
+    command_output_with_timeout(
+        &mut windows_player_command(path),
+        WINDOWS_AUDIO_PLAYER_TIMEOUT,
+        "Windows MediaPlayer",
+    )
+    .map_err(|e| format!("Windows MediaPlayer playback failed: {e}"))
 }
 
 #[cfg(not(any(windows, target_os = "macos")))]
@@ -205,61 +210,70 @@ impl AudioPlayer {
     }
 
     fn output_with_timeout(self, path: &Path, timeout: Duration) -> std::io::Result<Output> {
-        let mut child = Command::new(self.program)
-            .args(self.args)
-            .arg(path)
-            .stdin(std::process::Stdio::null())
-            .stdout(std::process::Stdio::piped())
-            .stderr(std::process::Stdio::piped())
-            .spawn()?;
-        let Some(stdout) = child.stdout.take() else {
-            terminate_and_reap(&mut child)?;
-            return Err(std::io::Error::other("audio player stdout was not piped"));
-        };
-        let Some(stderr) = child.stderr.take() else {
-            terminate_and_reap(&mut child)?;
-            return Err(std::io::Error::other("audio player stderr was not piped"));
-        };
-        let stdout_reader = read_output(stdout);
-        let stderr_reader = read_output(stderr);
-        let deadline = Instant::now() + timeout;
-
-        loop {
-            match child.try_wait() {
-                Ok(Some(status)) => {
-                    let (stdout, stderr) = finish_output(stdout_reader, stderr_reader)?;
-                    return Ok(Output {
-                        status,
-                        stdout,
-                        stderr,
-                    });
-                }
-                Ok(None) => {}
-                Err(wait_err) => {
-                    let cleanup_result = terminate_and_reap(&mut child);
-                    let _ = finish_output(stdout_reader, stderr_reader);
-                    cleanup_result?;
-                    return Err(wait_err);
-                }
-            }
+        let mut command = Command::new(self.program);
+        command.args(self.args).arg(path);
+        command_output_with_timeout(&mut command, timeout, self.program)
+    }
+}
 
-            let now = Instant::now();
-            if now >= deadline {
+#[cfg(not(target_os = "macos"))]
+fn command_output_with_timeout(
+    command: &mut Command,
+    timeout: Duration,
+    description: &str,
+) -> std::io::Result<Output> {
+    let mut child = command
+        .stdin(std::process::Stdio::null())
+        .stdout(std::process::Stdio::piped())
+        .stderr(std::process::Stdio::piped())
+        .spawn()?;
+    let Some(stdout) = child.stdout.take() else {
+        terminate_and_reap(&mut child)?;
+        return Err(std::io::Error::other("audio player stdout was not piped"));
+    };
+    let Some(stderr) = child.stderr.take() else {
+        terminate_and_reap(&mut child)?;
+        return Err(std::io::Error::other("audio player stderr was not piped"));
+    };
+    let stdout_reader = read_output(stdout);
+    let stderr_reader = read_output(stderr);
+    let deadline = Instant::now() + timeout;
+
+    loop {
+        match child.try_wait() {
+            Ok(Some(status)) => {
+                let (stdout, stderr) = finish_output(stdout_reader, stderr_reader)?;
+                return Ok(Output {
+                    status,
+                    stdout,
+                    stderr,
+                });
+            }
+            Ok(None) => {}
+            Err(wait_err) => {
                 let cleanup_result = terminate_and_reap(&mut child);
                 let _ = finish_output(stdout_reader, stderr_reader);
                 cleanup_result?;
-                return Err(std::io::Error::new(
-                    std::io::ErrorKind::TimedOut,
-                    format!("{} playback timed out after {timeout:?}", self.program),
-                ));
+                return Err(wait_err);
             }
+        }
 
-            std::thread::sleep((deadline - now).min(AUDIO_PLAYER_POLL_INTERVAL));
+        let now = Instant::now();
+        if now >= deadline {
+            let cleanup_result = terminate_and_reap(&mut child);
+            let _ = finish_output(stdout_reader, stderr_reader);
+            cleanup_result?;
+            return Err(std::io::Error::new(
+                std::io::ErrorKind::TimedOut,
+                format!("{description} playback timed out after {timeout:?}"),
+            ));
         }
+
+        std::thread::sleep((deadline - now).min(AUDIO_PLAYER_POLL_INTERVAL));
     }
 }
 
-#[cfg(not(any(windows, target_os = "macos")))]
+#[cfg(not(target_os = "macos"))]
 fn read_output<R>(mut reader: R) -> std::thread::JoinHandle<IoResult<Vec<u8>>>
 where
     R: Read + Send + 'static,
@@ -271,7 +285,7 @@ where
     })
 }
 
-#[cfg(not(any(windows, target_os = "macos")))]
+#[cfg(not(target_os = "macos"))]
 fn finish_output(
     stdout_reader: std::thread::JoinHandle<IoResult<Vec<u8>>>,
     stderr_reader: std::thread::JoinHandle<IoResult<Vec<u8>>>,
@@ -285,7 +299,7 @@ fn finish_output(
     Ok((stdout, stderr))
 }
 
-#[cfg(not(any(windows, target_os = "macos")))]
+#[cfg(not(target_os = "macos"))]
 fn terminate_and_reap(child: &mut std::process::Child) -> std::io::Result<()> {
     if let Err(kill_err) = child.kill() {
         if child.try_wait()?.is_none() {
@@ -461,4 +475,23 @@ mod tests {
             "stderr should identify a MediaFailed error"
         );
     }
+
+    #[cfg(windows)]
+    #[test]
+    fn windows_audio_process_has_an_outer_timeout() {
+        let mut command = crate::noninteractive_process::command("powershell.exe");
+        command.args([
+            "-NoLogo",
+            "-NoProfile",
+            "-NonInteractive",
+            "-Command",
+            "Start-Sleep -Seconds 5",
+        ]);
+
+        let err =
+            command_output_with_timeout(&mut command, Duration::from_millis(50), "test player")
+                .expect_err("hanging Windows player should time out");
+
+        assert_eq!(err.kind(), std::io::ErrorKind::TimedOut);
+    }
 }
-- 
2.55.0.windows.3

